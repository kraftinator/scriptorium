# Generated data & outputs — regenerable, never in git
corpora/*/data/reels/
corpora/*/data/pages/
corpora/*/data/crops/
corpora/*/output/

# Per-strategy scratch dirs (crop PNGs + results.json — regenerable)
src/strategies/_*/
# Experiment scratch dirs (crop PNGs + results.json — regenerable)
experiments/*/_out/

# Python / venv
__pycache__/
*.pyc
.venv/
venv/

# Secrets — API keys, never in git
.env
*.env

# OS
.DS_Store
